# railway.yml

services:
  - name: spring-boot-app
    type: java
    env: openjdk-17
    build_command: ./gradlew build
    github:
      repo: yourusername/your-spring-boot-repo
      branch: main
    env_vars:
      - name: SPRING_PROFILES_ACTIVE
        value: production
